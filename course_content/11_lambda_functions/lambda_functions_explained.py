
# lambda are similar functions , they accept args
# lambda are one time use functions
# replacement for functions if that funcions are required only once
# anonymous functions
# syntax --> lambda <arguments> : expressions
# map, reduce, filter

def func(x):
    return x*2

print(func(10))

temp = lambda x : x*2

print(temp(10))





